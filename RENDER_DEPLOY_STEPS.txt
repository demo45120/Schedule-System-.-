🚀 SCHEDULE SYSTEM - RENDER DEPLOYMENT GUIDE

Your code is ready to deploy on Render! Follow these steps:

═══════════════════════════════════════════════════════════════

STEP 1: สร้าง Render Account
──────────────────────────────
1. เปิด: https://render.com
2. Click "Sign up"
3. เลือก "Use GitHub to sign up" หรือ "Use email"
4. ยืนยัน email

   (Free tier ได้ $5 credit/month พอเพียง)

═══════════════════════════════════════════════════════════════

STEP 2: Create New Web Service
──────────────────────────────
1. Dashboard → Click **"New +"** (ที่มุมขวา)
2. เลือก **"Web Service"**
3. Click **"Connect GitHub account"** และให้สิทธิ์

   (ระบบจะต้อง access GitHub repository)

═══════════════════════════════════════════════════════════════

STEP 3: เลือก Repository
─────────────────────────
1. ค้นหา: **"Schedule-System-.-"**
2. Click เลือก repository
3. ที่มุมขวา Click **"Connect"**

═══════════════════════════════════════════════════════════════

STEP 4: ตั้งค่า Deployment
──────────────────────────
ให้ Render auto-detect หรือตั้งค่าด้วยตนเอง:

**Name:** schedule-system (หรือชื่ออื่นที่คุณชอบ)

**Environment:** Node

**Build Command:**
   npm install

**Start Command:**
   node server.js

**Plan:** Free (หรือ Starter ถ้าต้องการ)

Click **"Create Web Service"**

═══════════════════════════════════════════════════════════════

STEP 5: รอให้ Build & Deploy เสร็จ
──────────────────────────────────
- Render จะเริ่ม build โดยอัตโนมัติ
- ดูความคืบหน้าใน **Logs** tab
- ควร takes 2-5 นาที

ดู status:
   ✓ กำลัง build... (Building)
   ✓ Build เสร็จ (Deployed)
   ✓ URL ปรากฏที่บนสุด

═══════════════════════════════════════════════════════════════

STEP 6: ตั้ง Environment Variables
──────────────────────────────────
1. ใน Render Dashboard เลือก Web Service ของคุณ
2. ไปที่ tab **"Environment"**
3. Click **"Add Environment Variable"**
4. เพิ่ม 2 ตัวแปร:

   KEY: SPREADSHEET_ID
   VALUE: (your-google-sheet-id)
   
   KEY: JWT_SECRET
   VALUE: (your-secret-key-here)

   KEY: GOOGLE_APPLICATION_CREDENTIALS
   VALUE: ./credentials.json

5. Click **"Save"**
6. Render จะ auto-redeploy ด้วยตัวแปรใหม่

═══════════════════════════════════════════════════════════════

STEP 7: ทดสอบ App
──────────────────
เมื่อ status เป็น "Live" ✓:

1. ดู URL ที่ Render ให้มา:
   https://schedule-system.onrender.com (ตัวอย่าง)

2. เปิดในเบราว์เซอร์

3. Login ด้วย:
   Username: admin
   Password: admin123

4. ควรเห็นหน้า Dashboard ✓

═══════════════════════════════════════════════════════════════

STEP 8: ตั้งค่าอื่นๆ (Optional)
──────────────────────────────

**Custom Domain:**
   Settings → Custom Domain → ใส่ domain ของคุณ

**Persistent Disk:**
   (สำหรับเก็บ data ถาวร)
   Settings → Disks → Add Disk

**Auto-Deploy on Push:**
   Default enabled - ทำการ push ไป GitHub
   Render อัตโนมัติ redeploy

═══════════════════════════════════════════════════════════════

⚠️  สำคัญ - credentials.json
──────────────────────────────
credentials.json ต้องอยู่ใน GitHub repository!

ทำไม: Render pull code จาก GitHub และต้องใช้ credentials
      เพื่อเข้าถึง Google Sheets

โครงการได้รับการ push credentials.json แล้ว ✓

═══════════════════════════════════════════════════════════════

TROUBLESHOOTING
───────────────

❌ "Build failed"
→ ดู Logs
→ ตรวจสอบ package.json ถูกต้อง
→ ตรวจสอบ npm dependencies

❌ "500 Internal Server Error"
→ ดู Logs ในระบบ
→ ตรวจสอบ environment variables
→ ตรวจสอบ SPREADSHEET_ID ถูกต้อง

❌ "Google Sheets error"
→ ตรวจสอบ SPREADSHEET_ID ถูกต้อง
→ ตรวจสอบ credentials.json อยู่ใน repo
→ Share Spreadsheet ให้ service account email

❌ "Port already in use"
→ ไม่เป็นปัญหา - Render assign port อัตโนมัติ

═══════════════════════════════════════════════════════════════

STATUS CHECKING
────────────────
Render Dashboard:

🟢 Live = ทำงานได้
🟡 Building = กำลัง build
🔴 Failed = มีปัญหา (ดู Logs)

═══════════════════════════════════════════════════════════════

AUTO-DEPLOY (ทำงานอัตโนมัติ)
─────────────────────────────
เมื่อคุณ push ไป GitHub:

git add .
git commit -m "Update schedule system"
git push

→ Render จะ detect การ push
→ Auto-deploy เริ่ม
→ ประมาณ 2-5 นาที ก็พร้อม

═══════════════════════════════════════════════════════════════

FINAL CHECKLIST
────────────────
✓ Render account created
✓ Web Service created
✓ GitHub connected
✓ Build Command: npm install
✓ Start Command: node server.js
✓ Environment variables set
✓ Deploy triggered
✓ Status: Live ✓
✓ App working in browser

═══════════════════════════════════════════════════════════════

YOU'RE DONE! 🎉

Your Schedule System is now live on:
https://schedule-system.onrender.com

(URL may be different - check Render dashboard)

═══════════════════════════════════════════════════════════════
