🚀 SCHEDULE SYSTEM - RAILWAY DEPLOYMENT GUIDE

Your code is now ready to deploy! Follow these steps:

═══════════════════════════════════════════════════════════════

STEP 1: ไปที่ Railway
─────────────────────
1. เปิด: https://railway.app
2. Click "Start New Project"
3. Authorize ด้วย GitHub account

═══════════════════════════════════════════════════════════════

STEP 2: เลือก Repository
─────────────────────────
1. Click "Deploy from GitHub repo"
2. ค้นหา: "Schedule-System-.-"
3. Click "Deploy Now"

Railway จะเริ่ม auto-deploy ทันที ✓

═══════════════════════════════════════════════════════════════

STEP 3: ตั้ง Environment Variables
──────────────────────────────────
ไปที่ Railway Dashboard:

1. เลือก Service ที่สร้างขึ้น
2. Tab "Variables"
3. เพิ่ม 2 variables:

   SPREADSHEET_ID = your-google-sheet-id-here
   JWT_SECRET = your-secret-key-here

   (สำคัญ: GOOGLE_APPLICATION_CREDENTIALS=./credentials.json
    จะ read จาก file ใน repo อัตโนมัติ)

4. "Save" และ trigger redeploy

═══════════════════════════════════════════════════════════════

STEP 4: รอให้ Deploy เสร็จ
────────────────────────
- Railway จะ auto-detect Node.js project
- Install dependencies (~1 นาที)
- Start server (~30 วินาที)
- สีเขียว ✓ = Ready!

═══════════════════════════════════════════════════════════════

STEP 5: ทดสอบ App
─────────────────
Railway จะให้ Public URL เช่น:
   https://schedule-system.up.railway.app

เปิดในเบราว์เซอร์ และ login:
   Username: admin
   Password: admin123

═══════════════════════════════════════════════════════════════

⚠️  สำคัญ - เกี่ยวกับ credentials.json
─────────────────────────────────────────

credentials.json ต้องอยู่ใน GitHub repo!
(ปกติเก็บ secret file ไม่ push, แต่ครั้งนี้ต้องเพราะ Railway
 ไม่มีวิธีง่ายๆ ให้ upload file separately)

ทางเลือก (ถ้าเดือด credentials.json):
- ใช้ Google Cloud Secret Manager
- ใช้ Railway secrets ด้วย base64 encoding
- แต่ง่ายสุดคือ push credentials.json ไป private repo

═══════════════════════════════════════════════════════════════

STEP 6: ตั้งค่า Auto-Deploy (Optional)
────────────────────────────────────────
Railway สนับสนุน auto-deploy:
- ทุกครั้งที่ push ไป GitHub
- Railway จะ auto-redeploy
- ไม่ต้องทำอะไร มันเกิดขึ้นเอง

═══════════════════════════════════════════════════════════════

TROUBLESHOOTING
───────────────

❌ "Build failed"
→ ดู Logs: Railway Dashboard → Logs tab
→ ตรวจสอบ package.json มี dependencies ถูกต้อง

❌ "Google Sheets error"
→ ตรวจสอบ SPREADSHEET_ID ถูกต้อง
→ ตรวจสอบ credentials.json อยู่ใน repo
→ Share Spreadsheet ให้ service account email

❌ "Cannot find module 'express'"
→ รอให้ npm install เสร็จ

═══════════════════════════════════════════════════════════════

NEXT STEPS
──────────
✓ Code pushed to GitHub
✓ Deployment config ready
→ Go to https://railway.app and deploy!

═══════════════════════════════════════════════════════════════
